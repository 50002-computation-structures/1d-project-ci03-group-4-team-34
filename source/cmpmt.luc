module cmpmt (
    input a[32],   // 32-bit input
    input alufn[6], // Control signal: 0 for Bitwise NOT (~), 1 for Two’s Complement (-)
    output m[32]   // 32-bit output
) {
    always {
        if (alufn == 6b011011) {  
            m = ~a;  // NOT (~a)
        } else {
            m = -a;  // Two’s Complement (-a)
        }
    }
}
